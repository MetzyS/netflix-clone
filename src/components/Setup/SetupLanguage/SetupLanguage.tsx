import { LanguageSelectionType } from "../../../types/data";

const SetupLanguage = (props: { content: LanguageSelectionType }) => {
  console.log(props);
  return (
    <div>
      <span></span>
    </div>
  );
};

export default SetupLanguage;
